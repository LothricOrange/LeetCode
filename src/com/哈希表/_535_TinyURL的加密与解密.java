package com.哈希表;

public class _535_TinyURL的加密与解密 {

    String url;
    // Encodes a URL to a shortened URL.
    public String encode(String longUrl) {
        this.url = longUrl;
        return null;
    }

    // Decodes a shortened URL to its original URL.
    public String decode(String shortUrl) {
        return url;
    }
}

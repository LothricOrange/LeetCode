package com.字符串;

public class 面试题0807_无重复字符串的排列组合 {
    public String[] permutation(String S) {
        if (S == null) return null;
        int len = S.length();
        if (len == 0) return null;
        String[] strs = new String[0];
        for (int i = 0; i < len; i++) {
            char c = S.charAt(i);

        }
        return null;
    }
}
